import 'package:flutter/material.dart';

class StyleAnalysisHistory extends StatelessWidget {
  const StyleAnalysisHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
